## What this changes

<!-- One or two sentences. -->

## What you measured

This repository has one rule: **claims must be measured, not reasoned.** Nearly
every bug fixed here was invisible to reading and only appeared when something
external was confronted — the live addon, the real API, an actual export.

So: what did you run, and what did it print?

```
paste the actual output
```

- [ ] I ran `python3 tools/verify_docs.py` and it passes
- [ ] I ran `python3 tools/test_verify_docs.py` and every case passes
- [ ] If this touches Blender behaviour, I ran `tools/verify_blender.py` in Blender and pasted the output above
- [ ] If this adds or changes a check, it fails on the broken input **and stays silent on correct input**

## Anything you could not verify

<!-- Say so plainly. An unverified claim marked as unverified is useful; an
     unverified claim presented as measured is the thing this repo exists to
     prevent. -->
